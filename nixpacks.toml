[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['cd crypto-intelligence && pip install -r requirements.txt']

[start]
cmd = 'cd crypto-intelligence && gunicorn app:app --host 0.0.0.0 --port $PORT'
